import React from 'react'

const Results = ({ result }) => {
  return (
    <div>
      <center><h1>Results</h1></center>
      {results.map((results) => (
        <div class="card">
          <div class="card-body">
          </div>
        </div>
      ))}
    </div>
  )
};

export default Results